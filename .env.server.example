# ============================================================
# SERVER-ONLY SECRETS — never exposed to the browser.
# These are set as Supabase Edge Function secrets:
#   supabase secrets set --env-file .env.server
# Copy this file to .env.server and fill in real values.
# .env.server is gitignored.
# ============================================================

# Supabase (service role / secret key)
SUPABASE_URL=https://rizumeeeqojhxhaskbmx.supabase.co
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxxxxxxxxxxxxxxxxxx

# Kit (email) — v4 API
KIT_API_KEY=kit_xxxxxxxxxxxxxxxxxxxxxxxx

# Zoom — Server-to-Server OAuth app (fill when available)
ZOOM_ACCOUNT_ID=
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
# Comma-separated meeting IDs of the two recurring webinars
ZOOM_MEETING_IDS=
